from setuptools import setup

setup(
    name='django-raspberrypi',
    version='0.1',
    packages=['django_raspberrypi'],
    install_requires=[
        'Django',
        # Add other dependencies here
    ],
)
